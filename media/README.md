# Media

Store demo assets and experiment captures.

## Guidance
- Keep generated run captures in `media/runs/` (ignored except `.gitkeep`)
- Prefer compressed, reviewable artifacts linked to experiment entries
